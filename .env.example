# RecoveryLM Environment Variables

# Option 1: Direct API Access (for self-hosting)
# Get your key from: https://console.anthropic.com/
VITE_ANTHROPIC_API_KEY=your_api_key_here

# Option 2: Proxy Mode (for hosted version)
# If set, the app will use this URL instead of calling Anthropic directly.
# The proxy handles authentication, so no API key is needed on the client.
# VITE_API_PROXY_URL=https://your-proxy-url.run.app/v1
